<%= render :partial => '/admin/download/submenu' %>
<div id='records'>
  <% if searching? %>
    <h2><%= t('shared.admin.search.results_for', :query => params[:search]) %></h2>
    <% if @download_categories.any? %>
      <%= render :partial => "download_categories",
                 :collection => @download_categories %>
    <% else %>
      <p><%= t('admin.search_no_results') %></p>
    <% end %>
  <% else %>
    <% if @download_categories.any? %>
      <%= will_paginate @download_categories %>

      <%= render :partial => "sortable_list" %>

      <%= will_paginate @download_categories %>
    <% else %>
      <p>
        <strong>
          <%= t('.no_items_yet', :create => t('admin.blog.submenu.categories.new')) %>
        </strong>
      </p>
    <% end %>
  <% end %>
</div>
